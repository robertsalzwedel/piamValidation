# construct tooltips for interactive plots

construct tooltips for interactive plots

## Usage

``` r
appendTooltips(df)
```

## Arguments

- df:

  data.frame as returned from \`validateScenarios()\`
