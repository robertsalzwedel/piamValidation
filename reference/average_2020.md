# Average 2020 to smoothen Covid shock in historical data

Adds a new model for each model in reference data with smoothed 2020
period and name "\<model\>\_smoothed".

## Usage

``` r
average_2020(hist)
```

## Arguments

- hist:

  reference data as used in “validateScenarios()“
